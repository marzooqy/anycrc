[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "anycrc"
version = "1.4.0b2"
description = "The fastest Python CRC Library"
readme = "README.md"
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/marzooqy/anycrc"

#Build for only one version of python in each OS
[tool.cibuildwheel]
build = "cp313-*"
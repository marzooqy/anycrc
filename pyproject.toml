[build-system]
requires = ["setuptools", "cython"]
build-backend = "setuptools.build_meta"

[project]
name = "anycrc"
version = "1.3.4"
description = "The fastest general Python CRC Library"
readme = "README.md"
license = {text = "Zlib"}
requires-python = ">=3.7"
dependencies = ["bitarray==3.4.2"]

[project.urls]
"Homepage" = "https://github.com/marzooqy/anycrc"

[tool.cibuildwheel]
skip = "pp*"
